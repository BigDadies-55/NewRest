<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ניהול תפריט</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background-color: #e9ecef; padding: 20px; }
        .admin-panel { max-width: 600px; margin: auto; background: white; padding: 25px; border-radius: 10px; box-shadow: 0 0 20px rgba(0,0,0,0.1); }
        input, button { width: 100%; padding: 12px; margin: 10px 0; border: 1px solid #ccc; border-radius: 5px; box-sizing: border-box; }
        button { background-color: #27ae60; color: white; border: none; cursor: pointer; font-size: 1.1em; }
        button:hover { background-color: #219150; }
        .item-list { margin-top: 30px; border-top: 2px solid #eee; padding-top: 20px; }
        .item-row { display: flex; justify-content: space-between; align-items: center; padding: 10px; border-bottom: 1px solid #eee; }
        .delete-btn { width: auto; background: #c0392b; padding: 5px 10px; font-size: 0.9em; }
    </style>
</head>
<body>
    <div class="admin-panel">
        <h2>🛠️ ניהול מנות בתפריט</h2>
        <input type="text" id="itemName" placeholder="שם המנה">
        <input type="number" id="itemPrice" placeholder="מחיר (₪)">
        <input type="text" id="itemImage" placeholder="כתובת תמונה (URL)">
        <button onclick="addItem()">הוסף מנה לתפריט</button>

        <div class="item-list" id="adminList">
            </div>
    </div>

    <script>
        function addItem() {
            const name = document.getElementById('itemName').value;
            const price = document.getElementById('itemPrice').value;
            const image = document.getElementById('itemImage').value;

            if (!name || !price) {
                alert("נא להזין שם ומחיר");
                return;
            }

            const items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            items.push({ id: Date.now(), name, price, image });
            localStorage.setItem('restaurantMenu', JSON.stringify(items));
            
            // איפוס שדות
            document.getElementById('itemName').value = '';
            document.getElementById('itemPrice').value = '';
            document.getElementById('itemImage').value = '';
            
            renderAdminList();
        }

        function deleteItem(id) {
            let items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            items = items.filter(item => item.id !== id);
            localStorage.setItem('restaurantMenu', JSON.stringify(items));
            renderAdminList();
        }

        function renderAdminList() {
            const list = document.getElementById('adminList');
            const items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            
            list.innerHTML = '<h3>מנות קיימות:</h3>' + items.map(item => `
                <div class="item-row">
                    <span><strong>${item.name}</strong> - ₪${item.price}</span>
                    <button class="delete-btn" onclick="deleteItem(${item.id})">מחק</button>
                </div>
            `).join('');
        }

        renderAdminList();
    </script>
</body>
</html><!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ניהול תפריט</title>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background-color: #e9ecef; padding: 20px; }
        .admin-panel { max-width: 600px; margin: auto; background: white; padding: 25px; border-radius: 10px; box-shadow: 0 0 20px rgba(0,0,0,0.1); }
        input, button { width: 100%; padding: 12px; margin: 10px 0; border: 1px solid #ccc; border-radius: 5px; box-sizing: border-box; }
        button { background-color: #27ae60; color: white; border: none; cursor: pointer; font-size: 1.1em; }
        button:hover { background-color: #219150; }
        .item-list { margin-top: 30px; border-top: 2px solid #eee; padding-top: 20px; }
        .item-row { display: flex; justify-content: space-between; align-items: center; padding: 10px; border-bottom: 1px solid #eee; }
        .delete-btn { width: auto; background: #c0392b; padding: 5px 10px; font-size: 0.9em; }
    </style>
</head>
<body>
    <div class="admin-panel">
        <h2>🛠️ ניהול מנות בתפריט</h2>
        <input type="text" id="itemName" placeholder="שם המנה">
        <input type="number" id="itemPrice" placeholder="מחיר (₪)">
        <input type="text" id="itemImage" placeholder="כתובת תמונה (URL)">
        <button onclick="addItem()">הוסף מנה לתפריט</button>

        <div class="item-list" id="adminList">
            </div>
    </div>

    <script>
        function addItem() {
            const name = document.getElementById('itemName').value;
            const price = document.getElementById('itemPrice').value;
            const image = document.getElementById('itemImage').value;

            if (!name || !price) {
                alert("נא להזין שם ומחיר");
                return;
            }

            const items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            items.push({ id: Date.now(), name, price, image });
            localStorage.setItem('restaurantMenu', JSON.stringify(items));
            
            // איפוס שדות
            document.getElementById('itemName').value = '';
            document.getElementById('itemPrice').value = '';
            document.getElementById('itemImage').value = '';
            
            renderAdminList();
        }

        function deleteItem(id) {
            let items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            items = items.filter(item => item.id !== id);
            localStorage.setItem('restaurantMenu', JSON.stringify(items));
            renderAdminList();
        }

        function renderAdminList() {
            const list = document.getElementById('adminList');
            const items = JSON.parse(localStorage.getItem('restaurantMenu')) || [];
            
            list.innerHTML = '<h3>מנות קיימות:</h3>' + items.map(item => `
                <div class="item-row">
                    <span><strong>${item.name}</strong> - ₪${item.price}</span>
                    <button class="delete-btn" onclick="deleteItem(${item.id})">מחק</button>
                </div>
            `).join('');
        }

        renderAdminList();
    </script>
</body>
</html>
